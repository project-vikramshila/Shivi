# Updates Module

This module manages auto-update behavior, update channels, and plugin state deployment.

- `src/updates/autoUpdater.ts` initializes Electron auto updates.
- `src/updates/pluginUpdateManager.ts` manages plugin update state and validation.
